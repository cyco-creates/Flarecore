[project]
name = "comfyui-flarecore"
version = "0.1.0"
description = "Procedural, physically-plausible lens flare rendering nodes for ComfyUI. Pure PyTorch, no GL, no models."
authors = [{ name = "cyco.creates" }]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = ["torch", "numpy", "Pillow"]
keywords = ["comfyui", "lens-flare", "compositing", "vfx"]

[project.urls]
Repository = "https://github.com/cyco-creates/comfyui-flarecore"

[tool.comfy]
PublisherId = ""
DisplayName = "FlareCore"
Icon = ""
