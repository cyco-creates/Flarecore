# Flarecore
Depth-aware procedural lens flares for ComfyUI. Flares fade behind objects using your depth map, find their own light source or take yours, keyframe across a batch, and output a separate flare-only pass for external compositing. Parametric PyTorch math in linear light. No textures, no OpenGL, runs on CPU, CUDA or MPS.
